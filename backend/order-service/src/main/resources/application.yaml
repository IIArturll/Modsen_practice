spring:
  liquibase:
    enabled: true
  application:
    name: order-service
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 1
  jpa:
    hibernate:
      ddl-auto: none
server:
  port: 0
